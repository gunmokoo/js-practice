// for

const abcd = ["a", "b", "c", "d"];

for (let i = 0; i < abcd.length; i++) {
  console.log(abcd[i]);
}

// for each

abcd.forEach((item) => console.log(item));

// for of

for (let alpha of abcd) {
  console.log(alpha);
}

for (let text of "Hello") {
  console.log(text);
}

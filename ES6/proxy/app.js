// or operator ||

let a;
const b = 0;

console.log(a || "Hello!");
console.log(b || "Hi!");

// Nullish coalescing operator ?? (no iternet Explorer)

console.log(a ?? "Hello!");
console.log(b ?? "This is ??");
